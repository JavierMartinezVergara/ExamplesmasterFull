<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.mockup.allexamples">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RX.RX_Java"
            android:label="@string/title_activity_rx__java"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".retrofit.Retrofit" />
        <activity
            android:name=".JDBCImageSFTP.ImageFTP"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".notificaciones.Notificaciones" />
        <activity
            android:name=".sharedPreferencesP.SharedPreferences"
            android:theme="@style/SharepreferencesTheme" />
        <activity android:name=".pokemonRetrofit.PokemonRetrofit" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/SharepreferencesTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>